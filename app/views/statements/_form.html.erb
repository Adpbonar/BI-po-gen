
    <%= simple_form_for(@statement ,  :url => statement_url(@statement)) do |f| %>
        <%= f.input :terms %>
        <%= f.input :notes %>
        <%= f.submit class: 'btn btn-blue' %>
    <% end %>

